{
  "name": "prospera",
  "private": true,
  "version": "0.0.0",
  "description": "Prospera — React career platform. Monorepo: frontend (Vite) + server (Express/MongoDB).",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "preview": "npm --prefix frontend run preview",
    "server": "npm --prefix server start",
    "server:dev": "npm --prefix server run dev",
    "dev:all": "concurrently -n web,api -c blue,green \"npm --prefix frontend run dev\" \"npm --prefix server run dev\"",
    "install:all": "npm install && cd frontend && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
